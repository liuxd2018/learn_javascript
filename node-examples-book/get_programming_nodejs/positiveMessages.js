let messages = [
    "A change of environment can be a good thing!",
        "You will make it!",
            "Just run with the code!"
                ];
                // use `.load` to load into repl

messages.forEach(message => console.log(message))